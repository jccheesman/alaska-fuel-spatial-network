# Git LFS: every committed data zip (inputs + the frozen network-of-record).
# Set up BEFORE the first push; `git lfs migrate import` handles history if needed.
inputs/**/*.zip filter=lfs diff=lfs merge=lfs -text
final_network/*.zip filter=lfs diff=lfs merge=lfs -text
